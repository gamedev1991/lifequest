# Cross-origin isolation, required by expo-sqlite's WebAssembly build (it talks to
# the SQLite worker over SharedArrayBuffer). Read by Netlify / Cloudflare Pages.
# GitHub Pages ignores this file — public/sw.js covers that case instead.
/*
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Opener-Policy: same-origin
