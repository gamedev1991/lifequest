# Decision log

Why things are the way they are. These were deliberate choices (several from a two-architect
design debate on 2026-07-09, recorded in [PLAN.md](PLAN.md)) — don't reverse them in passing.
If one truly needs revisiting, ask the owner first.

| # | Decision | Why |
|---|---|---|
| D1 | **Foundation-first core, vertical slices after** — engine + data layer built and tested before any screen; every later migration ships together with the UI that reads it | The completions log is append-forever (no hard delete): wrong rows written early can never be cleaned up. Once the core was proven, runnable milestones beat big-bang phases |
| D2 | **expo-sqlite async API + `withExclusiveTransactionAsync` for XP writes** | Completion insert and character XP/level update must be atomic; the exclusive variant prevents statement interleaving |
| D3 | ~~**UUIDs via `expo-crypto` `randomUUID()`**~~ — **superseded 2026-08-02 by D20** | Was: already in the Expo SDK, so zero added install size and no `uuid` package or Hermes polyfill risk. Expo was removed entirely; ids now come from the platform's own `crypto.randomUUID()`. The *reasoning* survived the stack change — still no package, still no polyfill |
| D4 | **Local-time day keys, computed only in `src/engine/time.ts`** | `toISOString().slice(0,10)` shifts late-evening completions to the wrong day (UTC bug); ms-based day math breaks on DST. Centralizing makes the bug class un-writable |
| D5 | **Hand-rolled calendar (pure `monthGrid` + Pressable grid)** | `react-native-calendars` adds real bundle weight and fights the glow theme; a month grid is ~40 lines of tested pure code |
| D6 | **Migration 0001 contains ONLY Phase 1 tables** | Migrations are forward-only and immutable once shipped; guessing Phase 2/3 shapes early would freeze mistakes. Phase 2/3 tables arrive with their features, informed by tested engine code |
| D7 | **Stores may only `set()` from rows returned by query functions** | Structurally enforces write-to-DB-first (CLAUDE.md §3) — you cannot update UI state that wasn't persisted |
| D8 | **XP computed by callers (store→engine), passed into `logCompletion`** | Keeps queries thin and mechanical; all game math stays in the unit-tested engine |
| D9 | **Skips in their own table, not zero-XP completions** | `completions` keeps meaning "things done"; stats never need filter clauses; streak engine (Phase 2) reads completions alone (spec §4) |
| D10 | **`__DEV__` invariant: `character.total_xp === SUM(xp_awarded)` after every write** | The one derived-cache exception in the spec gets a tripwire so drift is caught at the write that caused it |
| D11 | **Due dates as YYYY-MM-DD text input + snooze as +1d/+1w buttons; no datepicker dependency** | Meets the §4 snooze requirement (push forward without touching history) at zero dependency cost. Revisit only if the owner wants a picker |
| D12 | **Default expo-router tab icon font stubbed via metro config; inline SVG icons instead** | ~1MB install-size saving; spec mandates inline SVG iconography and forbids icon-font packages (§5) |
| D13 | **Undo = delete a specific completion row and subtract exactly its `xp_awarded`** | Log-time XP decisions (esp. counted tasks) make undo trivially exact; no recomputation, no retro-adjust on target edits (§7) |
| D14 | **`.npmrc` legacy-peer-deps; jest pinned to 29** | Not choices — forced by the Expo SDK 57 ecosystem (see GOTCHAS.md 1–2). Revisit on SDK upgrade |
| D15 | **The web build (installable PWA) is now the primary distribution channel; the APK path stays supported but is no longer how the owner gets the app** | The self-built APK never worked on the owner's device, and every fix cycle needed a laptop, a 93MB sideload, and "allow unknown apps". A URL updates itself and installs to the home screen in two taps. Same codebase — `app/` and `src/` are untouched by platform |
| D16 | **Kept expo-sqlite on web (WebAssembly + OPFS) rather than swapping in a web storage layer** | One data layer, one migration runner, one set of queries, one XP invariant — a second storage backend would fork the thing the whole app's correctness rests on. Cost is the cross-origin-isolation requirement, which D17 absorbs |
| D17 | **Cross-origin isolation supplied by our own service worker, not by host headers** | SQLite-wasm needs `SharedArrayBuffer`, which needs COOP/COEP response headers, which GitHub Pages cannot set. A ~70-line worker re-serves same-origin responses with those headers — and doubles as the offline cache, so hosting stays free and account-free. `public/_headers` still covers hosts that can set headers |
| D18 | **Web export uses `output: "static"`, not `"single"`** | `app/+html.tsx` (PWA metadata, worker registration, no-flash dark background) is only honored under static rendering. It also pre-renders each route, so a cold load paints a themed spinner instead of a blank page |
| D19 | **Exclusivity on web rebuilt in JS (`src/db/transaction.ts`), never by downgrading to `withTransactionAsync`** | Web has no `withExclusiveTransactionAsync` (one shared worker connection). Plain transactions would keep atomicity but drop non-interleaving, which is exactly what the D10 XP invariant depends on. A promise chain restores it without weakening native, which still uses the real exclusive transaction |
| D20 | **Expo removed entirely; web-only React + Vite + Tailwind + Magic UI** (2026-08-02, owner's call) | The owner wanted Magic UI components usable "as is". They are React-DOM + Tailwind + framer-motion and cannot render against React Native primitives, so keeping Expo would have meant maintaining two UIs for one app. Web was already the primary channel (D15), so native was dropped rather than duplicated. §2 is untouched: still offline-first, no accounts, no backend |
| D21 | **The rewrite replaced `app/` and `src/components/` only — `src/engine/`, `src/types/`, `src/store/`, and every SQL statement moved unchanged** | This is the §3 "hard rule" (all game math is pure TypeScript, no framework imports) paying for itself: a total UI-framework change touched none of the correctness-critical code, and all 63 engine tests passed on the new runner without edits |
| D22 | **A `SqlDatabase` interface (`src/db/sqlite.ts`) mirroring the four expo-sqlite methods actually used** | Every file in `db/queries/` and `db/migrations/` kept compiling across a total driver swap; only `client.ts` changed. Keeping the old method names is deliberate — the point is that the seam, not the vocabulary, is what matters |
| D23 | **`opfs-sahpool` VFS in a dedicated worker, replacing the plain `opfs` VFS** (supersedes D17) | Plain OPFS reaches its worker over `SharedArrayBuffer`, which requires cross-origin isolation, which requires COOP/COEP headers GitHub Pages cannot set — the whole reason D17's service worker had to forge them. sahpool uses synchronous access handles instead and needs no isolation at all, so that entire workaround is deleted. It must run in a worker regardless, because `createSyncAccessHandle()` is worker-only |
| D24 | **`base: '/lifequest/'` unconditionally in `vite.config.ts`, not `command === 'build'`** | Making it build-only meant `vite preview` served at `/` while the built HTML requested `/lifequest/…`; the SPA fallback answered those asset requests with `index.html` and the app booted to a silent blank page. One path everywhere means dev, preview, and production agree on the router basename, the service-worker scope, and every asset URL |

| D25 | **React Router 8 (package `react-router`, not `react-router-dom`), React pinned up to 19.2.8** | `npm audit` flagged a high-severity advisory covering react-router 7.12.0–8.2.0 (RSC-mode CSRF). It does not apply here — no server, no RSC, no actions — but there is no patched 7.x, so the choice was "ship a red audit" or upgrade. v8 drops the `react-router-dom` package entirely (imports move to `react-router`) and requires react ≥19.2.7, making this a three-package change. Taken because only the stable declarative API is used (`BrowserRouter`, `Routes`, `Route`, `NavLink`, `Link`, `useNavigate`, `useParams`, `useLocation`), the React bump is patch-level within 19.2.x, and the full browser harness re-verified every route, deep link, and the two-tab case afterwards. `npm audit` is now clean |

| D26 | **Adopted the Solo Leveling reference's *structure*, kept §5's electric-blue primary — and took its "hardcore" framing as visuals only** (2026-08-02, owner's calls) | The owner supplied two reference images (a Solo Leveling quest-system poster for the look, the Kamui landing page for motion). Both were taken selectively, and both edits are the kind that read as an accident later if unrecorded. **(a)** The poster is violet/magenta-forward; §5 specifies `#4C8DFF`. The owner chose to keep blue, so the frames, quest rows, stat block and radar were rebuilt in the existing palette — a reference is an input to the spec, not a replacement for it, and §5 is unamended. **(b)** The poster's "EVERY SKIPPED QUEST = PERMANENT XP LOSS / NO RESET BUTTON" panel contradicts §2's forgiving, non-punishing progression. The red panel was built as *visual language* and wired to something honest (quests still unclaimed today); **no XP is ever deducted anywhere**, and §7 is untouched. **(c)** The Kamui reference is a desktop scroll-jacked marketing site; porting its section choreography would have put an animation in front of the §2 "<5s to capture" goal. Only the vocabulary was taken — 3D card flip, blur-in headings, depth cross-fade, ambient parallax — applied to moments that already existed |

| D27 | **Every ornament is code-drawn; `src/components/system/` holds the new primitives, `src/components/ui/` stays untouched** | The reference is dense with frames, corner brackets, rune dividers, a portrait and a radar — the obvious implementation is images, which would grow an asset budget forever and break §3/§5's vector-only rule. All of it is CSS `clip-path` + inline SVG instead: `SystemPanel` gets its gradient edge from a two-layer sandwich (a gradient-background outer with 1px padding wrapping the panel fill, both clipped by the same chamfer polygon) because a gradient cannot be a border, and the portrait becomes a generated `Sigil`. The new primitives live in their own folder because `components/ui/` is vendored Magic UI kept diffable against upstream (CONVENTIONS 14b). Net cost of the whole redesign: **+0.8 KB gzipped JS, +1.8 KB gzipped CSS**, zero new dependencies, and all five routes still separately code-split |

| D28 | **GSAP added alongside `motion` (core + Flip + SplitText + DrawSVG, ~41 KB gz), amending §3's "one animation dependency" rule** (2026-08-02, owner's call) | The owner asked for an awards-level revamp and named GSAP. The rule it breaks was the owner's own, so it was put to them with measured numbers rather than either quietly broken or quietly refused. Three effects justify it: **Flip** (shared-element morphs), **SplitText** (per-character title reveals — doing this with `motion` means hand-splitting strings and managing the stagger yourself, and losing the `revert()` that hands the DOM back to screen readers as a word rather than a row of letters), and **DrawSVG** (borders that draw themselves on). `motion` cannot be dropped in exchange: every vendored Magic UI primitive is built on it, and replacing them would fork them from upstream (CONVENTIONS 14b). Cost is real and recorded — initial JS ~122 KB gz → ~163 KB gz. **Note what this decision is not:** it is not permission to animate more. The paired amendment (CONVENTIONS 13b) makes "nothing animates at rest" a hard rule, because the previous regression was 862 idle animations, not too few |

| D29 | **Streak state is derived from the completions log on every launch, never incremented** | A counter-based streak ("user completed something, add one") is correct only while the app is open. Miss four scheduled days without launching LifeQuest and a counter never notices — the streak would still read 4 when the user returns. Deriving means the walk from the task's start day to today *finds* the breaks, so being away is not a special case and needs no background job, no notification permission, and no "last seen" bookkeeping. Cost is one pass over `completions` per launch and per write: ~1,800 rows/year, a millisecond. When that stops being cheap the fix is a date-bounded read, not a counter |
| D30 | **`streaks` stores only what cannot be re-derived; `streak_resets` is keyed `UNIQUE(streak_id, day)`** | §7 says `longest_streak` "is a running max and is never decremented" and `reset_count` is a lifetime total — neither survives derivation, because undoing today's completion would erase a record set months ago. Those two columns are the reason the table exists; `current_streak` is mirrored for future SQL queries but the engine's value always wins. The unique key on resets is what makes re-deriving history idempotent: without it, every launch would re-insert every historical break and `reset_count` — a number the user sees — would inflate forever. `reset_count` is then read back from those rows rather than incremented, so the counter and the history it summarises cannot disagree |

| D31 | **Dashboard bars are one hue; the eight skill colours carry identity only next to a name label** | Running the `dataviz` validator over the app's eight skill colours on the dark panel surface failed three checks — worst all-pairs CVD ΔE **2.5** (Stock Trading ↔ Exercise, effectively identical for deuteranopes) and a normal-vision worst of **12.1** against a hard floor of 15, meaning some pairs are hard to separate even with full colour vision. A hill-climb search over hue/lightness/chroma, constrained to keep each skill recognisably its current hue, topped out at CVD ΔE **7.3** — still under the target of 8, and the best-scoring candidates were muted colours that would have gutted the §5 neon look. Eight categorical hues simply cannot pass all-pairs; that is why the guidance caps the ceiling at 7–8. So the fix is structural, not a repaint: ranking skills is a *magnitude* job, so the bars use a single accent hue and length does the comparing, while each skill's own colour appears on a marker that always sits immediately beside its name — colour **plus** label, never colour alone. The palette keeps its character and nothing on the screen depends on telling violet from blue |

| D32 | **Calendar backfill writes a real completion dated to the chosen day, with no date limit** (2026-08-02, owner's calls) | The use case is "I did this on Tuesday and forgot to log it", so a backfilled row has to behave like any other completion: same XP, same write path, counted by stats and by the streak engine — which means it can *repair* a break retroactively, since streaks are derived rather than incremented (D29). Two details make it safe. `completed_at` moves to the chosen day while `created_at` still records when the row was written, because the two answer different questions and collapsing them loses the second. And the timestamp is midday, not midnight, so a daylight-saving shift can never push the row onto the neighbouring day (D4's bug class, one level up). No date limit, by the owner's choice — it is their own data, and a limit would only make the honest case fiddly |
| D33 | **Exercise merged into Fitness rather than deleted** | The owner's note was "Exercise is a subset of Fitness, don't need both". Deleting the row would have silently destroyed XP the user earned, which is precisely what the append-forever completions log exists to prevent. Migration `0004` re-points every `task_skills` tag (OR IGNORE for tasks tagged with both, which would otherwise collide on the primary key), sums the two XP totals, prunes the dead id out of the MRU setting, and only then drops the row. Shipped migration `0002` still seeds Exercise and is deliberately left untouched — migrations are immutable once shipped (CONVENTIONS 18), so `0004` is the single source of truth for the end state |
| D34 | **The third design reference was taken for its structure and rejected for its skin** (2026-08-03) | The owner sent a pin with "make this look more cooler". Decoded, it is a 2020 nutrition tracker: light mode, mint green surfaces, an orange primary, photographic food thumbnails. Its *skin* contradicts §5 on every axis at once — and §5 is not a mood, it is the constraint that keeps the app assetless (§3). What the reference is genuinely good at is **hierarchy**: a week strip for date context, one big ring that hands its middle to the single number the screen is about, two flanking counters, three small meters, and a tinted hero shelf that the list scrolls against. All of that is layout, and layout transfers. So the layout was adopted wholesale and not one colour was. Same call as D26, third reference running — a moodboard is an input to the spec, not a replacement for it |
| D35 | **The ring's blue→violet ramp is computed per segment, not declared as an SVG gradient** | The obvious build is one `<linearGradient>` and every lit arc stroked with it. It looks wrong, and the reason is that a gradient is laid out across the element's **bounding box**, not along the path: the segment at twelve o'clock — the first one lit, the one the eye starts on — lands halfway down the diagonal and comes out violet. §5 wants blue leading and violet in its secondary role, so the colour is interpolated by segment index instead. The ring now starts on the primary and only reaches the secondary at the far end, and a segment keeps its colour as the ring fills rather than shifting under the user |
| D36 | **Category icons take the reference's icon *treatment*, not its artwork** (2026-08-03) | The owner pointed at the reference's category marks and asked for "that kind of icon". They are Fluent-3D-style rendered illustrations — soft-shaded, full-colour, one image per category. Shipping those means the app's first image asset, and then a growing one: §5 says vector/glow-panel only and §3 names "no custom illustrated art" as the reason the look survives contact with the bundle budget. What actually makes those icons work is separable from how they were made: they are **large**, they sit in a **dedicated framed cell**, they carry **their own colour** rather than the text colour, and they have **volume** instead of a uniform hairline. All four transfer to inline SVG. So the glyphs became duotone — a `currentColor` fill at 0.18 alpha under the stroke — and moved into `CategorySlot`, a notched tile tinted with the skill's stored colour. Same visual job, still zero bytes of asset. **If the owner wants literal 3D icons, that is a spec amendment to make deliberately, not a detail to slip in through an icon file** |
| D37 | **Depth is faked in vector; raster 3D icons were measured and left as an open owner call** (2026-08-03) | Asked for literal 3D icons, three objections were raised. Measuring them left **one and a half standing**. (1) *Colour identity* — a raster cannot be tinted, so a fixed-colour render breaks the rule that a skill's stored colour drives its slot, its bar and its radar axis. Half-answered: keeping the tint on the *slot* preserves most of it, but the side-by-side shows a red apple over a green Diet bar and a brown briefcase over a blue Career bar, so the clash is real and visible. (2) *Aesthetic fit* — warm matte renders on `#0A0E17` read as stickers rather than as part of a glowing HUD. This is a judgement, so it was settled with a rendered comparison rather than an assertion. (3) *Weight* — **this objection was wrong and is withdrawn.** The claim was that PNGs are heavy and cannot gzip. Gzip does nothing to them (measured: 5,820 B of WebP compresses to 5,761 B), but the premise was the problem: Microsoft's Fluent 3D set at 96px is ~1.5 KB per icon in WebP, so all eight cost **~11.6 KB** — noise next to a 168 KB bundle. Quoting a size objection without measuring it nearly killed a legitimate option on a made-up number. **Resolved 2026-08-03: the owner chose vector "for now" after seeing the side-by-side.** The raster path stays viable and costed — ~11.6 KB, plus a re-tint of the eight skill colours to match the renders, which is part of that job rather than an extra. Do not reopen this as a fresh question; it was decided on rendered evidence |
| D38 | **Badges store only the unlock; everything else about them is derived** (2026-08-03) | `badge_unlocks` has two columns, and the catalogue itself lives in `src/engine/badges.ts` — so adding, renaming or retuning a badge is a code change with no migration, which is what keeps 30 rules maintainable. Progress, tier and whether a badge *still* qualifies are all computed from the completions log on read (§4's never-store-derived-stats rule). The one thing that cannot be derived is that it was **earned**, because a badge is a one-way door: deleting today's completion must not revoke something won in March. That asymmetry is not new — §7 already makes `longest_streak` a running max that never decrements — and `newlyUnlocked` takes the already-earned set as an argument precisely so the rule is enforced at the seam rather than remembered at each call site |
| D39 | **Badge rules are a `measure`/`target` pair, not an `isUnlocked` predicate** | The obvious catalogue shape is `{ key, name, isUnlocked(snapshot) }`. It answers exactly one question. Splitting it into "reduce the snapshot to a number" plus "the number to reach" answers three from the same data: unlocked is `measure >= target`, the progress bar is `measure / target`, and a locked badge can say "6 / 10 quests" with no badge-specific UI anywhere. A gallery of locked badges that will not tell you how close you are is a tease, and with predicates the only way to fix that is a second hand-written progress function per badge — thirty chances for the two to disagree |
| D40 | **The category icon library is local and finite, not an icon API** (2026-08-03, owner asked for either) | The owner offered both options: "a predetermined list, or find options based on keywords at run time." A keyword lookup service is the nicer-sounding one and it is unavailable here — §2 says the app makes **no network calls**, not "none while offline", and an icon that fails to load on a plane is worse than one that was never offered. So the search is real but runs against a 30-entry index compiled into the bundle: every glyph carries keywords, "gym" finds the dumbbell, "spanish" the language mark. Two things fall out of it that a service would not have given: the icon a category shows can never change out from under it, and the picker can *pre-select* from the name as the user types, so most categories never need it opened at all |
| D41 | **The iPhone failure was answered with a diagnostic, not a guessed fix** (2026-08-03) | A second user hit a hard startup failure on iOS Safari, and the error — "The operation failed for an unknown transient reason (e.g. out of memory)" — is WebKit's generic `DOMException` text. It names nothing. There is **no WebKit in this build environment**, so any iOS-specific fix would be written blind and shipped unverified, which is how a plausible non-fix gets mistaken for a resolution. So the response was split: the two changes that are safe *and* independently justified were made (a smaller sync-access-handle pool, and one retry), and everything else was turned into a **probe** that runs the real OPFS sequence on the device that is failing and reports which step breaks. The next screenshot from that phone is now a diagnosis instead of a guess. **Whether iOS becomes a supported platform is deliberately still open** — that is a scope decision with a real cost, and it belongs to the owner, not to a bug report |
| D42 | **`clearOnInit` is never a recovery path** | The sahpool VFS takes a `clearOnInit` option that makes it open cleanly every time, and it is the obvious-looking fix for "the pool will not open". It works by **deleting the pool** — every quest, completion and XP row, with no server to restore from (§2). It is called out in a comment at the retry site precisely because the retry loop is where a future reader will be most tempted to reach for it. A startup failure the user can retry beats a startup success that silently erased their year |
| D43 | **Durable storage is requested, never assumed, and the real answer is shown** | OPFS holds the only copy of everything (§2 rules out a server), and a browser treats origin storage as best-effort by default — it may evict the whole origin under disk pressure, silently. `navigator.storage.persist()` asks for the durable bucket instead. Three details make it honest rather than decorative. It is **fired at boot but never awaited**: Firefox prompts, and awaiting would hold the app on its spinner behind a dialog. The grant is **never ours to make** — Chrome infers it from engagement, Safari grants it on home-screen install — so the outcome is *rendered* on Profile as `persisted` / `best-effort` / `unsupported` rather than assumed. And it is **not a backup**: a request the browser may refuse, and can still revoke when the user clears site data, is why Phase 3's JSON export/import moved to the front of the queue |

**Superseded by the 2026-08-02 web rewrite (D20):** D2, D3, D12, D14, D16, D17, D18, D19 were all
Expo/React-Native-specific and no longer describe the code. They are kept for the reasoning, not as
current guidance. The XP-write invariant they protected (D10) is intact — `src/db/transaction.ts`
still serializes every write, now because the sahpool VFS is single-connection.
